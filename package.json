{
  "name": "marcus-results-site",
  "type": "module",
  "version": "1.0.0",
  "description": "Marcus Results marketing site — built with Eleventy, deployed to Cloudflare Pages",
  "scripts": {
    "dev": "eleventy --serve --port 3000",
    "build": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "clean-css": "^5.3.3",
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.48.0"
  }
}
